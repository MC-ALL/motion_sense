[build-system]
requires = ["setuptools>=82.0.1", "wheel>=0.47.0"]
build-backend = "setuptools.build_meta"

[project]
name = "motion-sense-gateway-edge-processor"
version = "1.0.0rc1"
description = "Async edge processor for the motion_sense gateway stack"
readme = "README.md"
requires-python = ">=3.13,<3.15"
dependencies = [
  "aiomqtt==2.5.1",
  "fastapi==0.136.1",
  "httpx==0.28.1",
  "pydantic==2.13.3",
  "PyYAML==6.0.3",
  "uvicorn[standard]==0.46.0",
]

[project.optional-dependencies]
dev = [
  "pytest==9.0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
