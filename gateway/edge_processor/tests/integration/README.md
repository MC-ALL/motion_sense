# gateway edge_processor integration tests

本目录预留给 `gateway/edge_processor` 的端到端联调测试，用于验证 `mosquitto`、`influxdb`、`edge_processor` 与 backend 之间的完整链路。

当前状态：目录已保留，建议后续把 `deployment/gateway/container/verify_gateway_resilience.sh` 中的关键场景逐步沉淀为自动化集成测试。
