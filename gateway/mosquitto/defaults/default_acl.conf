# Development default. Replace with a stricter ACL before production rollout.
# Rules must be scoped to an authenticated user; rules before a `user` block
# only apply to anonymous clients in Mosquitto.
user __MOSQUITTO_USER__
topic readwrite gym/#
