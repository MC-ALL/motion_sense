[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "motion-sense-backend-api-service"
version = "0.1.0"
description = "Async backend API service for the motion_sense platform"
readme = "README.md"
requires-python = ">=3.13,<3.15"
dependencies = [
  "aiomqtt==2.4.0",
  "fastapi==0.115.12",
  "pydantic==2.11.4",
  "psycopg[binary,pool]==3.3.3",
  "PyYAML==6.0.2",
  "redis[hiredis]==7.3.0",
  "uvicorn[standard]==0.34.2",
]

[project.optional-dependencies]
dev = [
  "httpx==0.28.1",
  "pytest==8.3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
