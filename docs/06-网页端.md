# 06 — 网页端

## 定位

面向健身房管理员、教师和学生的 Web 可视化平台，提供实时数据仪表盘、历史数据查询、告警管理和运维健康视图。业务数据通过后台端获取，基础设施健康视图通过独立运维观测端获取。

---

## 技术栈

| 层次 | 选型 |
|------|------|
| 框架 | React 19（hooks） |
| 语言 | TypeScript 5 |
| 构建工具 | Vite |
| 路由 | React Router v7 |
| 状态管理 | Zustand |
| 数据可视化 | 自定义 SVG 时序图 + SVG/HTML 状态卡片 |
| HTTP 客户端 | Axios（统一处理 JWT） |
| UI 组件库 | Ant Design 5 |
| 实时通讯 | 原生 WebSocket API |
| 样式 | 自定义 CSS + Ant Design token |

---

## 主要页面

### 1. 实时仪表盘

- 所有业务实时数据通过后台 `WS /api/ws` 更新。
- 页面当前聚焦离线态势、环境均值、能耗汇总与绑定关系，不再承载高频业务时序图。
- 顶部卡片支持展开二级视图，绑定关系卡片支持直接定位到下方绑定视图。

### 2. 器材管理

- 采用“总览页 + 实例详情页”两级结构：`/equipment` 与 `/equipment/{device_id}`。
- 总览页展示自定义器材卡片；实例详情页展示实时功率、累计能耗、绑定手环与历史曲线。

### 3. 手环管理

- 采用“总览页 + 实例详情页”两级结构：`/wristband` 与 `/wristband/{device_id}`。
- 总览页展示自定义手环卡片；实例详情页展示生命体征曲线、绑定状态、绑定历史与配置下发。

### 4. 环境质量

- 采用“总览页 + 实例详情页”两级结构：`/env-quality` 与 `/env-quality/{device_id}`。
- 总览页展示自定义环境节点卡片；实例详情页展示趋势图、超标提示与聚合统计。

### 5. AI 分析报告

- 接口位置已预留，当前仍未实现。
- 页面设计保留入口，但不纳入当前开发重点。

### 6. 告警管理

- 支持按 `level`、设备、确认状态筛选。
- 支持单条确认与批量确认。

### 7. 用户管理

- 管理员视角的用户、角色、设备注册管理。
- 已接入后台 `GET/POST/PATCH/DELETE /api/v1/users`，提供管理员用户列表、创建、改角色/密码、删除，以及 `gym_ids` / `device_ids` 归属编辑。
- 已接入后台 `GET/POST/PATCH/DELETE /api/v1/devices`，提供设备注册、元数据维护、所属网关映射与删除。

### 8. 系统健康中心

用于查看网关、后台及其关键组件的在线与健康状态。

页面建议布局：

```text
┌──────────────────────────────────────────────────────────────┐
│ [模块总数] [健康模块] [降级模块] [离线模块]                  │
├──────────────────────────────────────────────────────────────┤
│ gateway/gw-001   healthy   更新时间: 09:00:00               │
│  - gateway_api        healthy   8ms                         │
│  - edge_processor     healthy   4ms                         │
│  - mqtt_broker        healthy   3ms                         │
│  - local_timeseries   degraded  120ms                       │
├──────────────────────────────────────────────────────────────┤
│ backend/api-main offline   更新时间: 08:59:30               │
│  - api_service        offline   timeout                     │
└──────────────────────────────────────────────────────────────┘
```

- 汇总卡片展示健康、降级、离线模块数量。
- 模块列表展示整体状态、最近上报时间、异常组件数。
- 详情展示组件状态、检查时间、延迟、异常详情。
- 当前设计默认优先使用 REST 轮询：
  - `GET /api/v1/ops/health`
  - `GET /api/v1/ops/health/{module_id}`
  - `GET /api/v1/ops/alerts`
  - `PATCH /api/v1/ops/alerts/{id}/close`
  - `GET /api/v1/ops/stats`
- 若需要实时刷新，可接入 `WS /api/ws/ops`。

---

## 当前状态

- 已完成：实时仪表盘、器材总览/实例详情、手环总览/实例详情、环境总览/实例详情、告警管理、系统健康中心的基础版本。
- 已完成：后台 JWT 登录、独立登录页 `/login`、Token 持久化、REST Bearer 注入与业务 WebSocket `token` 透传。
- 已完成：接入 `ops_observer` 聚合健康接口、运维告警关闭接口与 `WS /api/ws/ops`。
- 已完成：管理员入口仅显示用户管理、设备注册、系统健康中心；教师 / 学生在器材、手环、环境实例页隐藏配置下发控件，仅保留只读快照；学生隐藏告警管理入口，直链访问返回无权限提示。
- 已完成：前端会话模型与用户管理页已同步支持 `gym_ids` / `device_ids` 归属映射。
- 已完成：通用时序图切换为自定义 SVG 实现，避免重型运行时与整图重绘。
- 未完成：AI 分析报告实际功能。

## 当前能力清单

| 页面 / 能力 | 当前状态 | 说明 |
|------------|---------|------|
| 实时仪表盘 | 已实现基础版本 | 已接入业务 `WS /api/ws`、设备快照、离线态势、环境均值、能耗汇总与绑定关系视图 |
| 器材管理 | 已实现基础版本 | 已拆分为总览页与实例详情页；详情页已接入历史功率/次数曲线、当前绑定手环快照与 `target_reps` 下发 |
| 手环管理 | 已实现基础版本 | 已拆分为总览页与实例详情页；详情页已接入心率/步数/电量曲线、绑定状态、绑定历史与配置下发 |
| 环境质量 | 已实现基础版本 | 已拆分为总览页与实例详情页；详情页已接入环境趋势图、超标提示与 `telemetry_interval_s` 下发 |
| 告警管理 | 已实现基础版本 | 已接入查询、按级别/设备/确认状态筛选、单条确认与批量确认；学生角色隐藏该页 |
| AI 分析报告 | 未满足 | 当前仅保留接口与页面位置 |
| 用户管理 | 已实现基础版本 | 已接入管理员用户列表、创建、改角色/密码、删除，以及 `gym_ids` / `device_ids` 归属编辑；仅管理员可见 |
| 设备注册 | 已实现基础版本 | 已接入管理员设备列表、注册、更新元数据、所属网关映射与删除；仅管理员可见 |
| JWT 鉴权 | 已实现基础版本 | 前端已支持登录、Token 本地持久化、REST Bearer 注入与 WS `token` 透传 |
| 系统健康中心 | 已实现基础版本 | 已接入 `ops_observer` 健康汇总、详情、告警查询/关闭与运维 WebSocket；仅管理员可见 |

## 与后台与运维端连接规范

### 业务后台

- REST API：`Authorization: Bearer {jwt}`
- WebSocket：`wss://{host}/api/ws?token={jwt}`
- 断线后指数退避重连（`1s -> 2s -> 4s`，上限 `30s`）
- 未登录时，实时仪表盘、器材总览、手环总览、环境总览、告警管理、设备注册页仅显示登录提示，不主动请求受保护接口，也不建立业务 WebSocket
- 当前前端已支持：
  - 登录：`POST /api/v1/auth/login`
  - Token 刷新：遇到 `401` 时调用 `POST /api/v1/auth/refresh`
  - 退出：`POST /api/v1/auth/logout`
  - 运行时注入 `backend_base_url`、`backend_ws_url`
  - 独立登录页：`GET /login`，支持携带来源页面并在登录成功后回跳

### 运维观测端

- REST API：`Authorization: Bearer {jwt}`，复用后台登录得到的管理员 access token
- WebSocket：`wss://{host}/api/ws/ops?token={jwt}`
- 健康中心不直接访问数据库，也不直连网关或后台私有端口。

完整契约见 [07-通讯接口定义.md](./07-通讯接口定义.md)。

---

## 权限与角色

| 角色 | 可访问页面 |
|------|-----------|
| 管理员 | 全部页面，含用户管理、设备注册、配置下发、运维视图 |
| 教师 | 仪表盘、器材、环境、告警；器材/环境配置为只读；数据范围受 `gym_ids` / `device_ids` 限制 |
| 学生 | 仪表盘、器材、环境；仅显示 `device_ids` 明确绑定的设备数据 |

## 页面验收矩阵

| 页面 | admin | teacher | student |
|------|-------|---------|---------|
| 实时仪表盘 | 查看全部实时卡片与告警流 | 仅查看 `gym_ids` / `device_ids` 范围内设备 | 仅查看 `device_ids` 范围内设备 |
| 器材管理 | 查看全部器材；允许下发 `target_reps` | 仅查看授权器材；隐藏配置面板，仅保留只读快照 | 仅查看授权器材；隐藏配置面板，仅保留只读快照 |
| 手环管理 | 查看全部手环；允许下发心率阈值与 Notify 周期 | 仅查看授权手环；隐藏配置面板，仅保留只读快照 | 仅查看授权手环；隐藏配置面板，仅保留只读快照 |
| 环境质量 | 查看全部环境节点；允许下发 `telemetry_interval_s` | 仅查看授权环境节点；隐藏配置面板，仅保留只读快照 | 无场馆继承，仅在设备被显式授权时可见；隐藏配置面板，仅保留只读快照 |
| 告警管理 | 查看全部告警；允许单条与批量确认 | 仅查看授权范围内告警；允许确认 | 不可见；直链访问返回无权限提示 |
| 用户管理 | 可见；允许管理账号和归属 | 不可见 | 不可见 |
| 设备注册 | 可见；允许注册、修改、删除 | 不可见 | 不可见 |
| 系统健康中心 | 可见；允许查看运维健康与告警 | 不可见 | 不可见 |

验收要点：
- 页面顶部登录态应显示当前角色与有效数据范围。
- 业务页只读提示应明确说明当前账号的可见范围。
- 前端显示范围必须与后台返回结果保持一致，不能仅依赖前端本地过滤。
- 运维健康中心与用户管理、设备注册均必须由后台管理员登录后才发起请求。

---

## 部署

前端构建产物 `dist/` 由 Nginx 静态托管：

- 镜像与默认配置位于 `deployment/web/portal_app/`。
- 模块最小 Compose 样例位于 `deployment/web/compose/docker-compose.yaml`。
- 整栈正式入口位于 `deployment/compose/docker-compose.yaml`。
- 统一运行时目录位于 `deployment/runtime/`。
- `runtime_config.js` 由容器入口脚本在首次启动时生成，修改后通常只需重启网页端容器。

- `GET /` → React SPA
- `GET /api/*` → 代理至后台业务 API 或运维观测 API
- WebSocket Upgrade 由 Nginx 透传
